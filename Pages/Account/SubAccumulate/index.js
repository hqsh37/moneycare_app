import { Text, View, StyleSheet } from "react-native";

const SubAccumulate = () => (
  <View style={styles.wrapper}>
    <Text>Accumulate Screen</Text>
  </View>
);

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default SubAccumulate;
